<Window x:Class="GymApp.View.DetailsPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GymApp.View"
        mc:Ignorable="d"
        Title="DetailsPage" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Firstname -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Ime:" VerticalAlignment="Center" FontWeight="Bold"/>
        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding FirstName}" VerticalAlignment="Center"/>

        <!-- Lastname -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Prezime:" VerticalAlignment="Center" FontWeight="Bold"/>
        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding LastName}" VerticalAlignment="Center"/>

        <!-- Payment Day -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Datum uplate:" VerticalAlignment="Center" FontWeight="Bold"/>
        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding PaymentDay}" VerticalAlignment="Center"/>
        <!-- Email -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Email:" VerticalAlignment="Center" FontWeight="Bold"/>
        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Email}" VerticalAlignment="Center"/>

        <!-- Expiry Date -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Datum isteka:" VerticalAlignment="Center" FontWeight="Bold"/>
        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding ExpiryDate}" VerticalAlignment="Center"/>

        <!-- Payment History -->
        <TextBlock Grid.Row="4" Grid.Column="0" Text="Istorija placanja:" VerticalAlignment="Top" FontWeight="Bold"/>
        <ScrollViewer Grid.Row="4" Grid.Column="1" VerticalScrollBarVisibility="Auto" Height="150">
            <ItemsControl ItemsSource="{Binding PaymentHistory}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Margin="5"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Notes -->
        <TextBlock Grid.Row="5" Grid.Column="0" Text="Beleske:" VerticalAlignment="Top" FontWeight="Bold"/>
        <TextBox Grid.Row="5" Grid.Column="1" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" 
                 Text="{Binding Notes, Mode=TwoWay}" Margin="0" />
    </Grid>

</Window>
