<Window x:Class="GymApp.View.LoginPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GymApp.View"
        xmlns:converter="clr-namespace:GymApp.Converter"
        mc:Ignorable="d"
        Title="LoginPage" Height="450" Width="800">

    <Window.Resources>
        <converter:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Width" Value="350"/>
           
            <Setter Property="Background" Value="#F1F1F1"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Regular"/>
            <Setter Property="Margin" Value="0 0 0 10"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- Style for rounded PasswordBox -->
        <Style x:Key="PasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Width" Value="350"/>
            <Setter Property="Background" Value="#F1F1F1"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Regular"/>
            <Setter Property="Margin" Value="0 0 0 10"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- Style for Login Button -->
        <Style x:Key="LoginButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="BorderBrush" Value="#4CAF50"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0 20 0 0"/>
            <Setter Property="FontSize" Value="16"/>
        </Style>
        <!-- Style for error message -->
        <Style x:Key="ErrorMessageStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="Red"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0 0 0 10"/>
        </Style>
    </Window.Resources>

    <!-- Layout of the Login Page -->
    <Grid Background="#F3F4F7">
        <!-- Main Login Container -->
        <Border Background="White" Margin="0,40,0,40" Padding="20">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <!-- Title -->
                <TextBlock Text="Welcome" FontSize="32" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

                <!-- Username TextBox with Placeholder -->
                <Grid>
                    <TextBlock Text="Username" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="15,0,0,0" Foreground="#AAA"/>
                    <TextBox x:Name="UsernameTB" Text="{Binding Username}" Style="{StaticResource TextBoxStyle}" Margin="0,0,0,10" GotFocus="TextBox_GotFocus" LostFocus="TextBox_LostFocus"/>
                </Grid>

                <!-- Password PasswordBox -->
                <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_PasswordChanged" Style="{StaticResource PasswordBoxStyle}" Margin="0,0,0,10" />
                <!-- Error message for Password -->
                <TextBlock Text="{Binding PasswordError}" Style="{StaticResource ErrorMessageStyle}" Visibility="{Binding PasswordError, Converter={StaticResource NullToVisibilityConverter}}" />

                <!-- Login Button -->
                <Button Content="Login" Command="{Binding LoginCommand}" Style="{StaticResource LoginButtonStyle}" HorizontalAlignment="Stretch"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
